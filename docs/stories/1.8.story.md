# Story 1.8 — ESLint + Prettier

**Epic:** 1.0 — Tabela de Desempenho: Produção & Segurança
**Status:** Done
**Complexidade:** S (Small)
**Agente:** @dev

## Objetivo
Configurar ESLint e Prettier para padronização de código, garantindo que o codebase existente passe sem erros críticos.

## Acceptance Criteria

- [x] AC1: ESLint instalado e configurado: `eslint`, `@eslint/js`, `typescript-eslint`, `eslint-plugin-react-hooks`
- [x] AC2: Prettier instalado e configurado: semi: true, singleQuote: true, tabWidth: 2, printWidth: 120
- [x] AC3: `eslint.config.js` (flat config) criado com regras para React + TypeScript
- [x] AC4: `.prettierrc` criado
- [x] AC5: Scripts adicionados ao `package.json`:
  - `"lint": "eslint ."`
  - `"lint:fix": "eslint . --fix"`
  - `"format": "prettier --write ."`
- [x] AC6: `npm run lint` executa sem erros de configuração (0 errors, 22 warnings)
- [x] AC7: Warnings existentes documentados (não bloqueantes): react-hooks/set-state-in-effect (App.tsx:30, hooks/useAppState.ts:28), no-create-in-render (components/ProgressBar.tsx:69), @typescript-eslint/no-explicit-any (múltiplos arquivos), @typescript-eslint/no-unused-vars (múltiplos arquivos)
- [x] AC8: `.eslintignore` criado — nota: ESLint 9+ emite aviso de deprecação para .eslintignore; `ignores` também configurado no `eslint.config.js`

## Arquivos Impactados
- `package.json` — devDependencies e scripts
- `eslint.config.js` — criar
- `.prettierrc` — criar
- `.eslintignore` — criar

## Notas Técnicas
- Usar flat config (novo padrão ESLint 9+)
- Não forçar fix automático no codebase existente nesta story — apenas configurar e documentar warnings
- Prettier e ESLint devem ser compatíveis (usar `eslint-config-prettier` se necessário)

## Dependências
- Nenhuma técnica direta

## Definition of Done
- `npm run lint` executa com sucesso
- `npm run format` formata o código
- Configuração não bloqueia o build
