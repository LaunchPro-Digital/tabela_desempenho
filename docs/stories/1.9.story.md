# Story 1.9 — Testes com Vitest

**Epic:** 1.0 — Tabela de Desempenho: Produção & Segurança
**Status:** Done
**Complexidade:** M (Medium)
**Agente:** @dev

## Objetivo
Implementar suite de testes unitários com Vitest cobrindo a lógica de cálculo de métricas e o hook de estado, garantindo uma base sólida para futuras mudanças.

## Acceptance Criteria

### Setup
- [x] AC1: Vitest instalado e configurado: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`
- [x] AC2: `vitest.config.ts` criado (ou configuração no `vite.config.ts`)
- [x] AC3: Scripts adicionados ao `package.json`:
  - `"test": "vitest run"`
  - `"test:watch": "vitest"`

### Testes — calculator.ts
- [x] AC4: Teste `calculateWeeklyValue()` para `PERCENTAGE_CUMULATIVE` — valores normais, zero total, zero success
- [x] AC5: Teste `calculateWeeklyValue()` para `SUM_TARGET` — soma correta
- [x] AC6: Teste `calculateWeeklyValue()` para `PERCENTAGE_AVERAGE` — média correta
- [x] AC7: Teste `calculateWeeklyValue()` para `MAX_LIMIT` — valor direto
- [x] AC8: Teste `calculateMetricStatus()` — retorna green/yellow/red corretamente para cada tipo
- [x] AC9: Teste `calculateMetricStatus()` com entries vazio — retorna `{ value: 0, status: 'gray' }`
- [x] AC10: Teste `getStatusColor()` — mapeia status para classes Tailwind corretas
- [x] AC11: Teste `getWeeklyHighlights()` (da Story 1.6) — ordenação, empate, top N

### Testes — useAppState.ts (básicos)
- [x] AC12: Mock do Supabase client configurado
- [x] AC13: Teste de carregamento inicial (loadState) com dados mockados
- [x] AC14: Teste de `updateEntry()` — atualização otimista do state local

### Cobertura
- [x] AC15: `npm run test` passa 100% sem falhas
- [x] AC16: Cobertura mínima: calculator.ts > 90%, useAppState.ts > 60%

## Arquivos Impactados
- `package.json` — devDependencies e scripts
- `vite.config.ts` ou `vitest.config.ts` — configuração test
- `services/__tests__/calculator.test.ts` — criar
- `hooks/__tests__/useAppState.test.ts` — criar
- `test/setup.ts` — setup global (jsdom, mocks)

## Notas Técnicas
- Vitest é compatível nativamente com Vite (zero config extra para paths, transforms)
- Mock do Supabase: criar factory que simula `supabase.from().select()`, `.upsert()`, etc.
- Usar `vi.mock()` para isolar Supabase client nos testes de useAppState

## Dependências
- Story 1.6 (getWeeklyHighlights deve existir para testar)
- Story 1.8 (ESLint configurado para manter testes limpos)

## File List
- `package.json` — devDependencies (vitest, testing-library, jsdom, coverage-v8) e scripts (test, test:watch, test:coverage)
- `vitest.config.ts` — criado: jsdom environment, setup file, coverage thresholds por arquivo
- `test/setup.ts` — criado: import jest-dom/vitest matchers
- `services/__tests__/calculator.test.ts` — criado: 43 testes (calculateWeeklyValue, calculateMetricStatus, getStatusColor, getWeeklyHighlights)
- `hooks/__tests__/useAppState.test.ts` — criado: 15 testes (loadState, updateEntry, saveFeedback, updateUsers, error handling)

## Dev Agent Record
### Results
- 58 testes passando (43 calculator + 15 useAppState)
- calculator.ts: 100% stmts, 93.24% branches, 100% funcs, 100% lines
- useAppState.ts: 90.36% stmts, 65% branches, 100% funcs, 97.1% lines
- Todos os thresholds de cobertura atendidos

## Definition of Done
- [x] `npm run test` passa sem falhas
- [x] calculator.ts com cobertura > 90%
- [x] Base de testes estabelecida para desenvolvimento futuro
